{
  "name": "poe-kr-en-dictionary",
  "version": "2.0.0",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.js",
    "build:poe1": "node scripts/build.js --version poe1",
    "build:poe2": "node scripts/build.js --version poe2",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "playwright": "^1.56.1"
  },
  "devDependencies": {
    "vitest": "^3.0.0"
  }
}
